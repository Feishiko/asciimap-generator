# Ascii Map Editor

## Install

You can just install the binary file from release, or if you like, you can use scoop to install

```sh
scoop bucket add feishiko https://github.com/Feishiko/scoop-feishiko
scoop install feishiko/ascii-map-generator
```

## Build from source

```sh
dotnet publish asciimapGenerate.sln
```